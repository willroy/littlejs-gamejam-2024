class PhysicsObjectEntity extends Entity {
  constructor(pos, size, rgba, world) {
    super(pos, size, rgba, world);
  }

  render() {
    super.render();
  }

  update() {
    super.update();
  }
}